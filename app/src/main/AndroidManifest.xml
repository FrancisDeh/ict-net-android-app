<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.francisdeh.ictnetapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Launcher"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".Classroom"
            android:launchMode="singleTop">
        </activity>
        <activity
            android:name=".VideoPlayer"
            android:parentActivityName=".Classroom">
        </activity>
        <activity
            android:name=".ImageView"
            android:parentActivityName=".Classroom">
        </activity>
        <activity android:name=".TermView"
            android:parentActivityName=".Classroom">
        </activity>
    </application>

</manifest>